# Sample Data Folder

Do not commit real patient images.

For local testing, place anonymized CT and PET NIfTI files here, for example:

- `ct.nii.gz`
- `pet.nii.gz`

The desktop application can select files from any local folder.
